do_training: true
max_iterations: int 1000
error_cutoff: float 8e-4
learn_rate: float 0.1
learn_decay: float 1
print_weights: false

# how often to print out training loss (in # of iterations)
printout_period: int 8
gain: float 1.8463

network_topology: int[16384,16,16,5]

add_noise: float 0.2
input_dropout: float 0.5

# "sigmoid", "tanh", or "leaky_relu"
activation_function: "sigmoid"

# "randomize", "from_file", or "fixed_value" (fixed_value is not implemented yet)
initialization_mode: "smart_random"

# load_file is not used with initialization_mode="randomize"
load_file: "network.net"
save_file: "network.net"

dataset_mode: "from_file"
dataset_file: "dataset/train.data"
